name: Sonarqube

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  sonarqube:
    runs-on: [self-hosted]

    steps:
      - uses: actions/checkout@v3
      - name: analysis
        run: ./gradlew sonarqube -x test
          #-Dsonar.github.oauth=${{ secrets.SONAR_ACCESS_TOKEN }}
